services:
  camera-monitor:
    build: .
    image: camera-monitor:latest
    network_mode: host
    command: uvicorn main:app --host 0.0.0.0 --port 8010
    volumes:
      - ./app/cameras.csv:/app/cameras.csv:ro
    restart: unless-stopped